@model IEnumerable<TrazabilidadGanadera.Areas.Bovino.Models.Bovino>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Bovinos";
}
<div class="container">
    <h2 class="page-header">Bovinos</h2>
    <div class="panel panel-default with-table">
            <div class="panel-heading">
                <div class="row">
                    <div class="col-sm-1">
                        <a class="btn btn-sm btn-primary btn-default" href="bovino/create">
                            <span class="glyphicon glyphicon-file"></span>
                            Nuevo
                        </a>
                    </div>
                </div>
            </div>
            <table class="table">
                <tr>
                    <th>Nombre</th>
                    <th>Categoría</th>
                    <th></th>
                </tr>
            @if (Model.Count().Equals(0))
            {
                <tr>
                    <td colspan="3">
                        No hay registros
                    </td>
                </tr> 
            }
            @foreach (var item in Model)
            {
                 <tr>
                     <td>
                         @item.Nombre
                     </td>
                     <td>
                         @item.CategoriaNombre
                     </td>
                     <td class="text-right">
                         <a href="bovino/detail?id=@item.Id" class="btn btn-default btn-xs">
                             <span class="glyphicon glyphicon-eye-open"></span>
                             Detalles
                         </a>
                         <a href="bovino/edit?id=@item.Id" class="btn btn-default btn-xs">
                             <span class="glyphicon glyphicon-pencil"></span>
                             Modificar
                         </a>
                         <a href="bovino/delete?id=@item.Id" class="btn btn-default btn-xs">
                             <span class="glyphicon glyphicon-remove"></span>
                             Eliminar
                         </a>
                     </td>
                 </tr>
            }
            </table>

    </div>
</div>

